import React from 'react';
import clsx from 'clsx';
import styles from './Board.module.scss';

const Board = () => {
  return <aside className={clsx(styles['board'])}>Board</aside>;
};

export default Board;
